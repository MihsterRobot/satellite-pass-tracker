{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "./Dockerfile"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn satellite_tracker.wsgi --bind 0.0.0.0:8000"
  }
}
