{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "./Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'python manage.py migrate && python manage.py collectstatic --noinput && gunicorn satellite_tracker.wsgi --bind 0.0.0.0:8000'"
  }
}
